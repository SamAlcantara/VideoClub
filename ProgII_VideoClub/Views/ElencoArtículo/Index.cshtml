@model IEnumerable<ProgII_VideoClub.Models.Elenco_articulo>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Rol)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Articulo1.Titulo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Elenco1.Nombre)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Rol)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Articulo1.Titulo)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Elenco1.Nombre)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Articulo }) |
            @Html.ActionLink("Details", "Details", new { id=item.Articulo }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Articulo })
        </td>
    </tr>
}

</table>
